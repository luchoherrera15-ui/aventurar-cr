// La implementación vive en `pantallas/explorar.tsx` (nombre heredado
// de cuando Eventos era la pestaña de aterrizaje): acá solo se expone
// como ruta apilada, ya que dejó de ser una página del pager.
export { default } from "@/pantallas/explorar";
