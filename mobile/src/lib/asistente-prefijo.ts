/**
 * Mismo marcador que `src/lib/asistente-prefijo.ts` en la web — el
 * asistente de IA antepone este texto a sus respuestas automáticas en
 * la tabla `mensajes`, compartida entre web y app.
 */
export const PREFIJO_ASISTENTE = "💎 ";
