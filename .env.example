NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Llave de servicio de Supabase (Project Settings → API → service_role).
# SOLO servidor, nunca en el cliente. Sin esto no se pueden crear cuentas
# desde el panel admin ni mandar los correos de reserva: el aviso al dueño
# necesita leer su correo en `perfiles`, que la anon key no puede ver.
SUPABASE_SERVICE_ROLE_KEY=

# Confirmaciones de reserva por correo (resend.com). Sin esta variable
# los correos simplemente no se envían — la reserva funciona igual.
RESEND_API_KEY=
# Remitente propio, una vez que verifiques tu dominio en resend.com/domains.
# OJO: sin esto se usa el remitente de prueba de Resend, que SOLO entrega a
# la casilla dueña de la cuenta — ni los clientes ni los dueños de los
# negocios reciben nada. Es la diferencia entre "me llega a mí" y "funciona".
RESEND_FROM_EMAIL=
# Casilla del equipo que recibe el aviso "entró un pedido de invitación".
# Sin esta variable el aviso NO se manda: el pedido queda bien guardado,
# pero nadie se entera hasta que alguien abra /admin/invitaciones.
BOOKEA_CORREO_PEDIDOS=
# OBLIGATORIA. Es la llave de los crons (/api/auto-cobro y
# /api/recordatorios): Vercel la manda en el header Authorization al
# dispararlos. Sin ella esas rutas RECHAZAN todo — o sea, los crons
# dejan de correr. Antes pasaba lo contrario y era peor: sin la variable
# la puerta se abría sola y cualquiera podía disparar un cobro.
# Generala con: openssl rand -hex 32
CRON_SECRET=
# Opcional: el dominio real del sitio, para los links del correo
# (botón, pie de página). Por defecto usa https://bookea.lat.
NEXT_PUBLIC_SITE_URL=
# ============================================================
# STRIPE — cobro con tarjeta de los paquetes de Lealtad (0143)
#
# TODO ESTO ES OPCIONAL. Sin estas variables no se dibuja ningún botón
# de tarjeta y la compra sigue siendo por SINPE con comprobante, igual
# que siempre. El plan `prueba` ($0) nunca pasa por Stripe.
#
# El paso a paso para llenarlas está en docs/stripe.md.
# ============================================================
# Llave SECRETA de la cuenta (Developers → API keys). Empieza con
# sk_test_ mientras se prueba y sk_live_ cuando se cobra de verdad.
# SOLO servidor: nunca con el prefijo NEXT_PUBLIC_.
STRIPE_SECRET_KEY=
# Signing secret del endpoint del webhook (Developers → Webhooks →
# el endpoint → "Signing secret"). Empieza con whsec_.
# SIN ESTO NO SE ACTIVA NINGÚN PLAN: el webhook rechaza todo lo que no
# pueda verificar, que es exactamente lo que tiene que hacer. Ojo: el
# secreto del modo prueba y el de producción son DISTINTOS.
STRIPE_WEBHOOK_SECRET=
# El mapeo precio → paquete. Son los ids `price_…` que Stripe da al
# crear cada precio (Product catalog → el producto → el precio).
# El código NO conoce ningún precio: si una de estas falta, ese paquete
# simplemente no se puede pagar con tarjeta (el botón no aparece), y si
# llega un cobro con un precio que no está acá, la suscripción queda
# registrada SIN activar nada y llega un correo al equipo.
STRIPE_PRICE_ARRANQUE_MENSUAL=
STRIPE_PRICE_ARRANQUE_ANUAL=
STRIPE_PRICE_IMPULSO_MENSUAL=
STRIPE_PRICE_IMPULSO_ANUAL=
STRIPE_PRICE_ILIMITADO_MENSUAL=
STRIPE_PRICE_ILIMITADO_ANUAL=

# ============================================================
# WALLET — Apple Wallet y Google Wallet de Bookea Lealtad
#
# Las 7 son las ÚNICAS que el código real lee (confirmado por barrido
# exhaustivo en la auditoría de Fase 0, docs/wallet-v2/auditoria-inicial.md
# sección 6). No existe ninguna variable APNS_* separada: el push reusa
# el mismo certificado del pase.
#
# Corré `npm run wallet:doctor -- --offline` después de llenarlas —
# valida formato, certificado↔llave, Pass Type ID↔certificado, Team
# ID↔certificado, cadena WWDR y vigencia, sin imprimir ningún secreto.
# ============================================================
# Certificado del Pass Type ID (el .p12 exportado del llavero,
# convertido a PEM) en Base64 de una sola línea.
APPLE_PASS_CERT_B64=
# Llave privada correspondiente, también PEM en Base64.
APPLE_PASS_KEY_B64=
# Certificado intermedio Apple Worldwide Developer Relations, PEM en
# Base64. Bajarlo de https://www.apple.com/certificateauthority/.
APPLE_WWDR_CERT_B64=
# El identificador con el prefijo "pass." (ej. pass.lat.bookea.afiliacion).
APPLE_PASS_TYPE_ID=
# El Team ID de la cuenta Apple Developer (10 caracteres).
APPLE_TEAM_ID=
# OPCIONAL (Wallet V2, Fase 2B) — sin esto, los pases nuevos siguen
# naciendo exactamente como siempre (token aleatorio guardado en
# claro). Si se define, los pases NUEVOS pasan a usar un token que se
# recalcula al vuelo (HMAC) y nunca se guarda — ver
# src/lib/wallet/config/auth-token.ts y
# docs/wallet-v2/decisiones-tablas.md §11. NINGÚN pase ya emitido
# cambia de token por esto. Generala con: openssl rand -base64 32
# — y NUNCA la reutilices como valor de otra variable.
APPLE_PASS_AUTH_SECRET_V1=

# Issuer ID numérico de Google Wallet Console.
GOOGLE_WALLET_ISSUER_ID=
# El JSON completo de la cuenta de servicio de Google Cloud (con
# permiso de Developer sobre ese Issuer), en Base64 de una sola línea.
GOOGLE_WALLET_SA_KEY_B64=

# Sal secreta del contador de visitas ("12 personas visitaron este sitio
# hoy"). Con ella el servidor convierte la IP + el navegador + el día en
# un hash: la IP en claro nunca se guarda. NUNCA con NEXT_PUBLIC_ — si
# la sal es pública, el hash deja de proteger nada. Sin la variable el
# contador sigue funcionando con una sal por defecto y avisa por consola.
# Generala con: openssl rand -hex 32
VISITAS_SALT=
