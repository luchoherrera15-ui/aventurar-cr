/**
 * El inicio ES el directorio: buscador segmentado, barra de categorías
 * y — sin filtros activos — rieles horizontales por categoría (la vista
 * "Todos"). Misma página en / y /ranchos-eventos para que los enlaces
 * viejos sigan funcionando.
 */
export { default } from "./ranchos-eventos/page";
